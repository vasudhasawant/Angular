import { AgeFromYMDPipe } from './age-from-ymd.pipe';

describe('AgeFromYMDPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeFromYMDPipe();
    expect(pipe).toBeTruthy();
  });
});
